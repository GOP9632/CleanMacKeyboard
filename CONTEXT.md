# CleanMacKeyboard

一個 macOS app，讓使用者在擦拭筆電時暫時停用輸入裝置，避免清潔過程誤觸鍵盤與觸控板。

## Language

### 狀態

**清潔模式**：
輸入裝置被本 app 攔截、使用者可以安全擦拭機身的狀態。放在待命、準備清潔旁邊講狀態機的階段時，同一件事叫**清潔中**，畫面上顯示的也是這三個詞。清潔模式期間螢幕不會因為閒置而變暗或關閉，因為擦機器的那幾分鐘裡使用者不碰鍵盤也不碰觸控板，而那正是系統判斷閒置的依據。這份宣告只擋閒置睡眠，不擋闔蓋睡眠，所以跟闔蓋解鎖不衝突。
_Avoid_: 鎖定畫面, 鎖屏, 螢幕鎖（這些指的是 macOS 內建的登入鎖定，本 app 完全不碰）

**待命**：
app 已啟動但未攔截任何輸入的狀態。

**準備清潔**：
使用者按下開始之後、攔截尚未生效的緩衝倒數。存在目的是讓使用者把手離開鍵盤、拿起抹布。可關閉，預設 3 秒。

**安全輸入模式**：
macOS 在密碼欄位取得焦點時啟動的保護機制，啟動期間所有第三方鍵盤攔截都會被繞過。對本 app 而言這是最危險的失效來源，因為它會讓清潔模式看起來正常但實際沒有生效。

### 攔截範圍

**鍵盤鎖**：
只攔截鍵盤事件的清潔模式。觸控板與滑鼠仍然可用，因此永遠保留一條非鍵盤的操作路徑。這是預設模式。

**全輸入鎖**：
同時攔截鍵盤、觸控板與滑鼠的清潔模式。點擊、拖曳與多指手勢都沒有作用，代價是沒有滑鼠可以求救。

攔的是事件，不是螢幕上那個箭頭：#12 在真機上驗過，游標還是會跟著手指移動，只是移到哪裡都按不動任何東西。游標的位置由視窗伺服器直接從 HID 那一層畫出來，不經過本 app 的攔截點。這是接受的行為，因為把箭頭真的定住的機制都是全域性的，會撞上「process 結束攔截必定消失」那條不變條件。畫面上的說明因此不承諾游標會停。

### 畫面呈現

**主視窗**：
app 唯一的常駐視窗，是一個普通視窗。清潔模式期間暫時置頂，避免背景 app 跳到前景把它蓋掉，大小與位置都不變。同一段時間裡關閉與縮小這兩顆按鈕會變灰，這是外觀唯一的變化：鍵盤鎖底下觸控板還活著，使用者點得到它們，而視窗一旦消失，他就在鍵盤還鎖著的狀態下失去唯一的狀態來源。這是預設呈現方式。

**遮蔽層**：
清潔模式期間額外鋪滿每一個螢幕的深色不透明畫面。除了顯示狀態，它同時提供隱私遮蔽，以及在輸入攔截失效時擋下誤點的第二層防護。固定深色、不跟隨系統外觀，因為它的用途之一是讓使用者看見螢幕上的灰塵。啟用時主視窗不做任何處置，自然被蓋住。每一個螢幕上顯示的都跟主視窗同一疊：圓環，加上攔截範圍與逾時剩餘時間那兩行。它把主視窗蓋掉之後，那一疊就是使用者僅剩的狀態來源，所以少一行都不行。

**授權引導畫面**：
還沒取得輔助使用授權時，主視窗裡取代圓環的那一整個畫面。它說明要去哪裡打勾、這個授權讓 Wipe 拿到什麼能力，以及 Wipe 只用它來丟棄事件、不記錄也不上傳。它刻意不留任何可以按的開始按鈕，因為一顆按了沒用的按鈕等於告訴使用者這個 app 壞了。授權拿到之後畫面自己換回圓環，不需要重開 app。
_Avoid_: 權限頁, 設定頁（它不是設定，是一次性的引導）

**圓環**：
主視窗與遮蔽層共用的主要顯示元件，中央顯示當前狀態文字。待命時是進入清潔模式的按鈕，清潔模式期間不可點擊，只負責顯示解鎖手勢的按住進度。逾時剩餘時間與目前的攔截範圍都以文字呈現在圓環外面，不佔用圓環。這兩行在待命時留著版位，主視窗的大小才不會因為進出清潔模式而改變。

### 離開清潔模式的方式

**解鎖手勢**：
使用者主動離開清潔模式的按鍵組合，需要持續按住一段時間才生效。
_Avoid_: 快捷鍵, hotkey（那是用來「進入」清潔模式的，方向相反）

**逾時解鎖**：
進入清潔模式後經過設定時間自動解除。預設 5 分鐘，上限 15 分鐘，不可關閉。存在目的是保險，不是正常使用路徑。

**闔蓋解鎖**：
偵測到螢幕蓋子闔上，或系統從睡眠喚醒時，自動回到待命狀態。以蓋子開闔狀態為主要訊號，睡眠喚醒為次要訊號，因為接上外接螢幕與電源時闔蓋不會睡眠。

## 不變條件

- 本 app 的 process 一旦結束，攔截必定消失。不做任何會在 app 死後仍留住鎖的事（不裝 daemon、不自動重啟）。
- 長按電源鍵強制關機與闔上螢幕是 macOS 保留的動作，本 app 攔不到，這是刻意保留的最終逃生出口。
- 安全網不做成設定。逾時解鎖、闔蓋解鎖、以及「解鎖手勢期間出現第三顆按鍵就重新計時」這三項一律鎖死，設定只調整體驗，不拆保險。
- 清潔模式絕不在無法可靠攔截時維持。偵測到安全輸入模式就拒絕進入，進行中偵測到就立刻退出；攔截建立不起來時同樣拒絕進入。寧可擾人，不可製造假象。
